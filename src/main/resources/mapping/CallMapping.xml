<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 命名空间 是必须的 -->
<mapper namespace="com.rong.ssm.mapper.CallMapper">
    <select id="selectByCallingPhone" parameterType="com.rong.ssm.vo.QueryCallVo"  resultType="com.rong.ssm.pojo.Call">
        select * from `call`
        where calling_nbr=#{callingPhone}
        <if test="calledPhone!=null">
            and called_nbr= #{calledPhone}
        </if>
        <if test="startTime!=null">
            and start_time &gt;= #{startTime,jdbcType=TIMESTAMP}
        </if>
        <if test="endTime!=null">
            and end_time &lt;= #{endTime,jdbcType=TIMESTAMP}
        </if>
    </select>
</mapper>
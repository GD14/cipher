<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 命名空间 是必须的 -->
<mapper namespace="com.rong.ssm.mapper.CustomerMapper">
    <select id="selectByPhone"  resultType="com.rong.ssm.pojo.Customer">
        select * from customer where cust_nbr=#{custNbr}
    </select>
</mapper>